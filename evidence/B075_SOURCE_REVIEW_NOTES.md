# B075 frozen-source review: pairs, truth values and relations

Frozen source revision `9620cc73f9c8e0ad003c514a5d3748f29611c4c0` governs both files. `OLP-0376` is `content/lambda-calculus/lambda-definability/pairs.tex`, 1,745 bytes, SHA-256 `4d2fd1df669f4a9a40328ebd0a7fcea1e38576e6b32acc5a5692626b934032cb`, with ten paired paragraph blocks. `OLP-0377` is `content/lambda-calculus/lambda-definability/truth-values.tex`, 3,170 bytes, SHA-256 `c59cf879d08ccb846c28886a957ffee1a5256157e9a160af104606743eb9006c`, with twelve paired blocks. Every source line was read against its staged Pashto draft. `B075_STAGING_QA.json` checks structure, identifiers, term tokens and exact math parity except the one narrowly enumerated `OLLAM-051` formula replacement. Frozen English bytes are unchanged.

- `OLP-0376:11–32` encodes an ordered pair as a function consuming a selector, then defines pair construction and first/second projections. The target preserves argument order, both projection lambda terms, and the exercise asking why they work.
- `OLP-0376:34–55` uses state pairs `(previous,current)` to compute predecessor: start at `(0,0)`, advance by shifting the second component and applying successor, then select the first component. The target distinguishes the zero case and the general `(n−1,n)` result. Its subtraction term applies predecessor to `a` exactly `b` times; the source formula is literal.
- `OLP-0377:12–22` encodes truth and falsity as two-argument selectors and gives their reductions on `M,N`. The target keeps the choice of first versus second argument.
- `OLP-0377:24–33` calls a relation lambda-definable when coded tuples reduce to the appropriate truth-value selector. The source writes `R⊆N^n` but the two displayed applications and condition all use `k` argument indices. `OLLAM-051` changes only that one arity exponent to `k` and discloses it in adjacent Pashto prose. The same letter `R` denotes both a relation and its representing term in the source; the target explicitly notes this overload without changing any other math span.
- `OLP-0377:35–46` gives `IsZero`: starting from true, zero iterations preserve true, while any positive number of iterations produces false. The target preserves this if-and-only-if explanation.
- `OLP-0377:48–70` defines negation by swapping selector arguments and conjunction by passing `y` and false to `x`. The target retains the Boolean-input assumption and the caveat about arbitrary lambda terms not necessarily yielding a truth value.
- `OLP-0377:72–76` asks for inclusive and exclusive disjunction. Both named functions and the encoding requirement remain in the exercise.

Reverse paraphrase: a Church pair stores two terms by feeding them to a supplied function; first and second projections choose the corresponding component. Iterating a state pair lets predecessor return the first component, and repeated predecessor gives subtraction. Boolean encodings choose between two inputs; `IsZero` is true exactly at zero, while `Not` and `And` operate on encoded truth values. A `k`-place relation is represented by true on its members and false otherwise. `OLLAM-051` repairs only the arity exponent; all other formulas remain literal. Pakistani scholarly phrasing is provisional where no exact technical term was attested. This is translation and source audit, not original mathematical research.
