# B071 frozen-source semantic review: eta and Church--Rosser

Revision `9620cc73f9c8e0ad003c514a5d3748f29611c4c0` is authoritative. The three English files remain byte-for-byte unchanged. `OLP-0366` is `content/lambda-calculus/syntax/eta.tex` (2,962 bytes; SHA-256 `a6027d321bb141166a89ae598a60f8cc559b5b74922b47f5932fe464a91eb978`); `OLP-0367` is `content/lambda-calculus/church-rosser/church-rosser.tex` (385 bytes; SHA-256 `f9a7362d873c4feed67ac0350ebb3c3cf3ddfd6046c4a7d0ef55e7086038ddf5`); `OLP-0368` is `content/lambda-calculus/church-rosser/definitions-and-properties.tex` (3,491 bytes; SHA-256 `8f2880db2bb1ecb7dc07bf844e6da844d82b48289a5f36270d20588d826deb6f`).

- `OLP-0366:12-24`: The introductory eta example means the same function only when the abstracted variable does not occur free in the function term; the source does not say this until the eta-contraction display at line 23. The target states the side condition in adjacent Pashto prose and preserves the display, with only its English `\text` argument localized (OLLAM-033). Eta contraction is one compatible step; beta-eta reduction is the reflexive-transitive closure of both beta and eta steps.
- `OLP-0366:37-67`: The eta conversion extension and the extensionality rule are distinct generators of extended equality. The latter requires the common argument variable to be absent from both terms. The theorem claims exactly the same induced equality relations. Function extensionality is phrased by agreement of values, consistent with the already recorded `TERM-FUNCTION-EXTENSIONALITY`; the source controls the actual proof.
- `OLP-0366:69-85`: The source uses `$ext$` and `$\equal[ext]$` where its earlier notation is `\ext` and `$\equal[\ext]$`. These math spans are preserved verbatim; an adjacent note explains their intended reference to the extensionality rule (OLLAM-034). The target preserves the proof's two containment directions and the free-variable hypothesis. It does not invent a new theorem.
- `OLP-0367:9-15`: This is a chapter driver. Only the chapter title is translated; all five imports and the end hook remain identical. The following imports are not yet all translated, so this driver is an editable draft, not a standalone accepted reader expansion.
- `OLP-0368:12-26`: The generic relation has the diamond property: two outgoing steps from one term can be completed to a common successor. The computational gloss must be read conditionally: confluence gives uniqueness of a normal form if it exists, not existence of a normal form. The target supplies that qualification in adjacent prose (OLLAM-035), aligning with the source's own lines 39-48.
- `OLP-0368:28-48`: The arithmetic paths both reach the common expression `12+3`. For beta reduction, the source takes the generic relation to be the beta-reduction relation, which admits the trivial zero-step reduction. Thus two reachable normal forms must coincide. The target retains all examples and the conditional scope.
- `OLP-0368:50-83`: The theorem propagates the diamond property to the transitive closure. Its proof fills an `(m+1)` by `(n+1)` grid with common-successor squares and yields a common endpoint. The source names the path endpoints `P` and `Q` only in its final sentence; the target identifies their role beside that sentence without altering any formula (OLLAM-036). The theorem's hypotheses, grid indices, bounds, and directional arrows remain exact.

Reverse paraphrase of the target: eta contraction removes a vacuous lambda abstraction when the bound variable is absent from the underlying function term; beta-eta reduction allows zero or more beta/eta steps. Extensionality equates functions that agree at a fresh argument; eta equality and extensional equality coincide by the two containments. A Church--Rosser diamond joins any two one-step successors; if a normal form is reachable it is unique. A finite grid of local diamonds joins any two finite reduction paths, so the transitive closure is Church--Rosser. These statements have been checked against the cited frozen lines; the exact eta and Church--Rosser Pashto compounds remain provisional.

No illustration requirement arises from this translation and source semantic review. Next: register the three drafts, four adjacent source disclosures, paragraph alignment, canon-use records, and cumulative QA.
