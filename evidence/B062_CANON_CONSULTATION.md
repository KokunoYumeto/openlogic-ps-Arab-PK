# B062 lambda-calculus source and language consultation

Scope: OLP-0341–0347 at frozen OpenLogic revision `9620cc73f9c8e0ad003c514a5d3748f29611c4c0`. The two source editorial notices keep their attribution and warn that the lambda part requires review and consolidation. The translation preserves the source's import structure and formulas, except the one disclosed `OLLAM-001` correction of an undefined body variable in the final Currying display.

The Pakistani Pashto prose passage `PK-IQRAM-P1-PROSE` (printed page 1, rendered image SHA-256 `c1cd7112ceaeef4e8cf4de1812d789e0ceec3561d378e1d0bdc6776de17ff83b`) was inspected for scholarly word order, کښې, and final ے. It does not attest lambda-calculus terminology. The regional Afghan passage `AF-NIAZMAN-P147-FUNCTIONS` (printed page 147, image SHA-256 `a6f9f85cbe0e0efab8b9ac82c3f0d10b106391a704fdda8ef589586ebb41d734`) was visually inspected: it explicitly uses تابع for function and تعويض for substitution, and distinguishes zero, successor and projection functions. Its regional role is a comparator, not evidence of a Pakistani formal standard. The previously inspected grammar passage `GRAMMAR-P166-SOV` (printed page 166, image SHA-256 `4ea5f09844b03ad466989401970a2ca49e9f94fd02375968d3285a0709a3592c`) supports verb-final exposition. Exact titles, source hashes and inspection metadata are in `CANON_SOURCES.jsonl` and `CANON_PASSAGES.jsonl`.

`TERM-LAMBDA-SYNTAX` and `TERM-LAMBDA-REDUCTION-CURRYING` are provisional for the exact abstraction, alpha/beta, redex, normal-form, Church–Rosser and Currying compounds. The established `TERM-SIMPLE-TYPE-THEORY`, `TERM-FUNCTION`, `TERM-FUNCTION-ARGUMENT` and `TERM-SYNTAX-SUBSTITUTION-PREDICATES` constrain their wording. Pure means no constants, whereas untyped allows arbitrary applications; one-step contraction differs from zero-or-more-step reduction. Currying returns functions as values instead of introducing primitive multi-argument abstraction.

The source's final Currying display substitutes into `P`, which has no binding in that general construction; the defined function body is `N`. The target substitutes into `N` and explains this adjacent to the display under `OLLAM-001`. The source file's `\olfileid{lam}{rep}{cur}` is retained exactly, even though the section is imported by the introduction chapter; this inherited structural oddity is not silently altered or counted as a correction. The B062 semantic-review entries in `work/batch01_consultation.json` give a source-to-target reverse paraphrase for each of the seven units. No human specialist approval is claimed.
