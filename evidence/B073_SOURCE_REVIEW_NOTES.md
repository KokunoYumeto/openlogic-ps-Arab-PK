# B073 frozen-source semantic review: Church--Rosser closure

Frozen source revision `9620cc73f9c8e0ad003c514a5d3748f29611c4c0` governs all three files. `OLP-0370` is `content/lambda-calculus/church-rosser/beta-reduction.tex`, SHA-256 `c51e50d16a675080277053003c33dc22951be9a43bb9910cc1bf1dff6e05f361`, 15 source paragraph blocks. `OLP-0371` is `parallel-beta-eta-reduction.tex`, SHA-256 `88130e362effc83767b2fae97dcab4b6ae13f657a7d7a43da2df13883c9d54f2`, 20 blocks. `OLP-0372` is `beta-eta-reduction.tex`, SHA-256 `2eee11e77e3bb3052a2e139079f26cb22c1126577b4418f88918a3ede85a3cf4`, 15 blocks. All source lines and all target blocks were reviewed; frozen English bytes remain unchanged. `B073_STAGING_QA.json` records exact structural, identifier, macro and math parity, with three enumerated formula corrections in OLLAM-041, OLLAM-044 and OLLAM-046.

- `OLP-0370:13–22` asserts any one-step beta contraction is one parallel beta step, but its proof explicitly covers only a root application redex. Since the earlier beta one-step relation is compatible, there are also abstraction and application-context cases. OLLAM-039 discloses and describes structural induction on the context. The target does not pretend the printed proof alone covered those cases.
- `OLP-0370:25–48` proves the reverse inclusion by induction on the last parallel rule. In rule 4's variable list at lines 41–44, the source repeats `$M'$` where the premise and ensuing contraction require `$N'$`. OLLAM-046 replaces that one literal inline math span and discloses it next to the case. All four induction cases and the sequence (body reduction, argument reduction, beta contraction) are retained.
- `OLP-0370:50–79` identifies ordinary multistep beta reduction as the smallest transitive relation containing parallel beta reduction, then derives Church--Rosser by the generic closure theorem and the parallel diamond. Because the ordinary reduction can use zero steps, OLLAM-040 makes explicit that reflexivity of parallel reduction supplies the zero-step case. The source's minor prose typo “Since is” is translated into grammatical Pashto without a mathematical change.
- `OLP-0371:13–36` defines parallel beta-eta reduction. Rule 2 again uses `$N\xrightarrow{\beta}N'$`, which blocks reflexivity under a normal abstraction. The target substitutes `$N\beredpar N'$` (OLLAM-041), as required by the reflexivity exercise and subsequent induction. The other four rules, including the eta side condition `$x\notin FV(N)$`, stay source-equivalent.
- `OLP-0371:44–59` defines beta-eta complete development. Its generic abstraction clause and final eta abstraction clause overlap when the latter's free-variable condition holds; the source supplies no precedence. OLLAM-042 says the conditional eta clause needs priority for a single specified development and for the proof at lines 83–90. The displayed source equations remain intact. This is a disclosed interpretation of an underspecified definition, not a claim that the original explicitly defines priority.
- `OLP-0371:61–98` reuses the four beta cases and treats the eta case. Its substitution argument tacitly depends on capture-avoiding substitution and alpha-renaming of the bound variable when necessary; these are governed by the earlier syntax/quotient treatment. The source's shortened proof and the incomplete “for some x and N-prime” list are preserved. The complete-development case explicitly uses the conditional eta clause and its precedence. No extra proof step is silently invented.
- `OLP-0372:11–25` uses the symbol `\beredone` in the one-step lemma, though the earlier eta section formally defines `\bered` as closure of beta and eta contraction without separately defining `\beredone` in prose. OLLAM-043 states the intended compatible one-step union beside the first use. The source then says `$M\bredone M'$` is an eta conversion; this is a wrong relation. OLLAM-044 changes exactly that math span to `$M\eredone M'$`, already defined in `syntax/eta.tex:18–23`. The printed proof again cites only the root eta case; OLLAM-045 points out the compatible-context cases and their structural induction. The beta cases refer back to `OLP-0370`.
- `OLP-0372:28–62` handles a parallel beta-eta step by a finite beta-eta reduction, uses the same transitive-closure argument, and invokes the generic closure theorem and parallel Church--Rosser theorem. Its eta case contracts the outer eta redex, then follows the inductive reduction of the body. The target preserves these directions, side conditions, labels and references. The proof remains as concise as the source, with its stated dependencies visible.

Reverse paraphrase: every ordinary beta step is a parallel beta step (also in a compatible context), every parallel beta step expands to ordinary beta steps, and their closures coincide, transferring Church--Rosser. Adding the eta rule gives a reflexive parallel beta-eta relation; conditional eta complete development must be chosen on overlapping abstractions for the common-successor proof. Finally every one-step beta/eta contraction embeds in parallel beta-eta, and each parallel step expands to ordinary beta/eta reduction, transferring the diamond to the ordinary reduction. The term names, priority interpretation and three exact formula repairs are open to specialist double-checking; no specialist approval is claimed. This work is translation and source review, not original mathematical research.
