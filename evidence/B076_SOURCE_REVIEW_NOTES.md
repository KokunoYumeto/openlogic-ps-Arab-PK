# B076 frozen-source review: primitive recursive functions in lambda calculus

Frozen source revision `9620cc73f9c8e0ad003c514a5d3748f29611c4c0` governs `OLP-0378`, `content/lambda-calculus/lambda-definability/primitive-recursive-functions.tex`, 5,835 bytes, SHA-256 `dbedc0b45f7d2ce9eebd36a219fe22b532ddebb6082fb4bf356afc612f4e294d`, with 21 paired paragraph blocks. Every source line was read against the Pashto draft. `B076_STAGING_QA.json` confirms structure, identifiers, term tokens, NFC and exact math parity apart from three specifically enumerated formula changes. Frozen English bytes are unchanged.

- `OLP-0378:10–29` states the primitive-recursive basis and gives explicit lambda terms for zero, successor and projections. The target uses the existing `TERM-PRIMITIVE-RECURSION` Pakistani Pashto decision and the earlier projection-function rendering; all three displayed terms remain literal.
- `OLP-0378:31–55` constructs composition from an outer k-place function and k inner n-place functions. The source lists representing terms through `$G_k$` although the inner functions and displayed construction end at index `k−1`; OLLAM-052 changes only that inline term to `$G_{k-1}$`. The conclusion says `$H$` is lambda-definable, but `H` is the representing term whereas the function under discussion is `h`; OLLAM-053 changes only that conclusion's inline symbol to `$h$`. The target discloses both adjustments adjacent to the lemma and retains the displayed construction, exercise and totality caveat.
- `OLP-0378:57–73` gives the primitive-recursion closure lemma. The source's second recursion equation places `h` on the right with n+2 arguments, contradicting the lemma's stated step function `g` and the proof's final `g(n,m,h(n,m))` calculation. OLLAM-054 changes only the right-hand function in that whole normalized `align*` math span to `g`, with an adjacent Pashto disclosure. The target describes iteration of the step function from the base value, rather than repeating the source's erroneous suggestion that h itself is the step function.
- `OLP-0378:75–94` defines a state pair using `D` and takes its second projection after iterating a Church numeral. The first pair component counts the current stage and the second carries the corresponding h value; the target keeps the single-parameter simplification, D substitution and induction claim. No unsupported assertion of a general n-parameter proof is added.
- `OLP-0378:96–122` proves the base case and induction step. The target keeps the zero case, induction hypothesis, each displayed reduction, the application of `G` to the parameter, stage and prior value, and the identity `g(n,m,h(n,m))=h(n,m+1)`.
- `OLP-0378:124–149` finishes with `H` reducing to the encoded h value, then states and proves closure of lambda-definable functions under the two formation operations. The long final calculation and all labels/references are literal. Its one English `by IH` within a source math span is retained as source formula material, not introduced target prose.

Reverse paraphrase: zero, successor and projections have lambda representatives. If f and each inner g are lambda-definable, their composition h has a representative H. For primitive recursion, the base value is F applied to the parameter; each Church-numeral iteration advances a pair of stage index and current h value using G. Induction yields `(m,h(n,m))`, so the second projection computes h. The basis plus closure under composition and primitive recursion establishes the proposition. Three source math defects are corrected in the target and disclosed; all other math and identifiers remain literal. This is translation and source audit, not original mathematical research.
