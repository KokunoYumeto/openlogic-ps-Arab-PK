# B077 frozen-source review: fixed points

Frozen source revision `9620cc73f9c8e0ad003c514a5d3748f29611c4c0` governs `OLP-0379`, `content/lambda-calculus/lambda-definability/fixpoints.tex`, SHA-256 `d826387d4284ef13679ef5b91a22bb7551c193c2e8c8356201ceb1df72a46b93`, with 18 paired paragraph blocks. Every source line was compared to the full Pashto draft. `B077_STAGING_QA.json` confirms structure, labels, term tokens, NFC and exact math parity except three enumerated source-to-target math spans. Frozen English bytes are unchanged.

- `OLP-0379:13–31` contrasts a self-referential factorial equation with ordinary abbreviations that can be expanded to pure lambda terms. The source repeats an alternate multiplication definition from OLP-0375 but again uses input `a` both as iteration count and summand, ignoring `b`. `OLLAM-055` changes only that displayed formula's summand to `b` and discloses it. The source's bare `0` is retained as its numeral shorthand; the target explicitly says numeral shorthand is expanded in obtaining a pure lambda term.
- `OLP-0379:33–46` unrolls the self-reference once. Its displayed two-line grouping closes the abstraction after the base branch before the recursive branch; as typeset it is schematic rather than a single well-grouped lambda term. `OLLAM-058` discloses this adjacent to the unchanged display. The target preserves the intended point: substituting the defining expression once still leaves a self-reference.
- `OLP-0379:48–63` says applying the factorial functional to a representative again represents factorial, then infers beta equality. Computing the same numeric function does not by itself imply beta equality of arbitrary representing lambda terms. `OLLAM-056` states this caveat adjacent to the passage; the target keeps the source claim as an attributed claim while distinguishing the fixed-point equation as an additional condition.
- `OLP-0379:65–86` gives Turing's Y combinator and a two-step reduction of `Yg` to `g(Yg)`. The target keeps the exact term, proof, beta equality and fixed-point meaning.
- `OLP-0379:89–111` describes an infinite unfolding sequence and observes that a different reduction path can still reach a normal form. The target keeps both claims and treats “infinitely many applications” as an intuition, not a literal completed term.
- `OLP-0379:113–137` works through factorial at three, two, one and zero with the Y combinator. All displayed reductions and branch cases remain literal; translated `intertext` clauses explain the steps.
- `OLP-0379:139–155` derives a general recursive term from `Y` and substitution. The target preserves the equation, term construction, every reduction and the beta-equivalence conclusion.
- `OLP-0379:157–173` compares Church's and Turing's combinators. The source calls the Church combinator weaker but writes `Yg` in the two comparison formulas, which names Turing's combinator and contradicts the immediately following `Y_C` proof. `OLLAM-057` changes only those two inline math spans to `Y_Cg` and `g(Y_Cg)`. The construction, common reduct `g(VV)` and final beta equality remain literal.

Reverse paraphrase: ordinary abbreviations can be replaced by their defining lambda terms, but simply unrolling a self-referential factorial equation never removes the self-reference. A fixed-point combinator supplies a term satisfying the recursive equation. Turing's displayed Y has a direct reduction to `g(Yg)`; the unfolding can be infinite even when another strategy terminates on a particular argument. The factorial example and general construction show the use of Y. Church's `Y_C` and its one-step unfolding have a common reduct with `g(Y_Cg)` rather than the same direct reduction. `OLLAM-055/057` make three exact formula changes; `OLLAM-056/058` are adjacent conceptual/source-display disclosures. Exact Pakistani fixed-point compound usage remains provisional. This is translation and source audit, not original mathematical research.
