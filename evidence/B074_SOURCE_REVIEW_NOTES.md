# B074 frozen-source review: lambda definability opening

Frozen source revision `9620cc73f9c8e0ad003c514a5d3748f29611c4c0` governs all three files. `OLP-0373` is `content/lambda-calculus/lambda-definability/lambda-definability.tex`, 704 bytes, SHA-256 `42df17a2d1c3ff9e2f53676b6bad10a9b10e7787f70d6371a2f60d0a22a78708`, with eight paired paragraph blocks. `OLP-0374` is `content/lambda-calculus/lambda-definability/introduction.tex`, 3,596 bytes, SHA-256 `ff7c8dba694a6e61b7d6445949a00ed6e61c5ddc1af1bcabcc7e0587ed1cb2d2`, with thirteen paired blocks. `OLP-0375` is `content/lambda-calculus/lambda-definability/arithmetical-functions.tex`, 4,990 bytes, SHA-256 `1c5db87c79c1edc7766be8031d7c6e35498be393367e2651c9b37950d5db7d24`, with eighteen paired blocks. All source lines were read. `B074_STAGING_QA.json` checks structure, identifiers, term tokens and exact math parity except two narrowly enumerated formula replacements (OLLAM-048 and OLLAM-050). Frozen English bytes are unchanged.

- `OLP-0373:9–16` identifies the chapter as experimental and in need of more explanation, proof organization and examples. The Pashto keeps this candid editorial caveat rather than presenting the chapter as finished. The chapter driver imports the listed sections in source order, including the commented-out lists import and end hook. It cannot yet be included in a coherent reader because later imported sections remain untranslated.
- `OLP-0374:12–24` distinguishes the untyped, abstract lambda calculus from arithmetic functions on natural numbers, then introduces a Church numeral for each natural number. The target does not suggest all lambda terms themselves denote arithmetic functions.
- `OLP-0374:26–38` defines the Church numeral as the iterator `\lambd[fx][f^n(x)]`, including zero and three examples. It is in normal form and accepts a function and an argument; the target keeps this two-argument behavior and all exact formula spans.
- `OLP-0374:40–59` says a term represents an arithmetic computation if its application to encoded input reduces to a Church numeral. Church--Rosser gives uniqueness of a normal form only if one exists; this does not assert termination. The target preserves this condition and the equivalence between the output numeral and function value.
- `OLP-0374:61–72` extends the definition to k-place partial functions. On defined input the application reduces to the corresponding numeral; on undefined input it has no normal form. The target preserves both branches, all argument indices and the original source's tokenized `lambda define`/`lambda definable` forms. This is the same technical sense already recorded in `TERM-LAMBDA-CHURCH-NUMERALS`.
- `OLP-0374:74–87` gives constant and identity functions and the computability equivalence. The source names a constant function `c_k` but writes its value equation as `c(n)=k`; OLLAM-047 discloses the mismatch in adjacent Pashto prose while preserving both formulas literally. It does not alter the mathematics of the constant-term example. The final equivalence is reported as the source's claim, not independently reproved here.
- `OLP-0375:13–35` defines a Church-numeral successor term and expands its action. The source calls the whole curried application a single redex and writes `\num{n}fx \redone f^nx`, although its own lambda convention expands the two-argument abstraction into nested one-argument abstractions. Two beta contractions are needed. OLLAM-048 changes only that inner math span to the multistep reduction arrow and discloses the reason; all other successor formulas and the explicit zero-to-one computation remain literal.
- `OLP-0375:59–88` gives two addition terms. The first and second displayed applications to two numerals use a one-step arrow despite the source's curried multiargument abbreviation. OLLAM-049 explains the suppressed successive beta contractions in adjacent Pashto prose; it leaves the two long formulas literal. The targets, argument order, `f^{n+m}` calculation and iteration of successor stay source-equivalent.
- `OLP-0375:90–124` gives a multiplication term, eta simplification and an alternate multiplication exercise. The main multiplication term iterates an m-fold function n times, yielding `nm`. The source's alternate exercise uses `a` both as iteration count and addition summand and ignores `b`, so it computes a square. For input a=2 and b=3 it returns 4, not 6. OLLAM-050 replaces only the inner addition argument with `b`, so the term iterates “add b” a times from zero. The adjacent Pashto note discloses that correction. The frozen source remains unchanged.
- `OLP-0375:125–143` gives two exponentiation terms and says predecessor/subtraction require pair encoding. The target retains the base/exponent argument order and both formulas. The source editorial note that eta reduction still needs explaining in this experimental chapter is translated as written, even though the earlier syntax chapter has already discussed eta; it is an editorial state of the source chapter, not a new mathematical claim.

Reverse paraphrase: Church numerals encode iteration by a lambda term; a term lambda-defines a partial arithmetic function exactly when defined coded inputs reduce to the right numeral and undefined coded inputs have no normal form. Church--Rosser rules out a second normal result when one exists. Successor adds one by applying a function once more; addition composes iterations; multiplication iterates an m-fold operation n times; exponentiation iterates the base numeral. The chapter remains experimental and incomplete. Exact Church-numeral and lambda-definability Pashto compounds are provisional. OLLAM-047/049 disclose two source notation/step-count issues without math changes; OLLAM-048/050 repair two exact math spans. This is translation and source audit, not original mathematical research.
